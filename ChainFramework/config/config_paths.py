# Configuration file with the needed paths
paths = {
    "dir": "", # working directory
    "input_subdir": "", # subdir where to find the PEs to path
    "hijacking_data_subdir" : "", # subdir where to find the hijacking data 
    "output_subdir": "", # subdir where to store the patching results
    "patched_subdir": "", # subdir where to store the patched PEs 
    "summary_filename": "", # filename where to store the patching summary
    # List of ignored PEs (e.g. PEs with integrity checks)
    "ignored_pes_path" : "", # ignored PEs list filename
}